//
// Created by Кирило Вєтров on 25.07.2026.
// Практика. Масиви, посилання та вказівники
//

#include <filesystem>
#include <iostream>
#include <utility>


int main() {

    // Посилання
    int num = 10;
    int& a = num;                                                   // & - посилання на адресу змінної, "int& a" або "int &a"
    a = 15;
    num = 20;

    std::cout << &num << " - " << num << std::endl;                 // виводимо адресу і значення змінна "num" посилається "a"
    std::cout << &a << " - " << a << std::endl;                     // виводимо адресу і значення

    // Вказівники
    int val = 12;
    int* ptr = &val;

    std::cout << &val << " - " << val << std::endl;
    std::cout << ptr << " - " << * ptr << std::endl;                // "ptr" вже містить адресу "val", тому замість "&ptr"-> "ptr"

    //      Задача 1 (Базова) - Gemini
    // Створи масив із 7 цілих чисел (значення заповни сам або зчитай через std::cin).
    // Знайди суму всіх елементів.
    // Обчисли їхнє середнє арифметичне (увага: результат має бути дробовим числом double).
    int arr1[7] = {1, 1, 3, 4, 5, 6, 7};
    int sum = 0;
    double avrg = 0;

    for (int i = 0; i < 7; i++) {
        sum += arr1[i];

    }
    avrg = sum / 7.0;
    std::cout << "Сума всіх елементів - " << sum << std::endl;
    std::cout << "Середнє арифметичне - " << avrg << std::endl;
    // -----------------------------------------------------------------------------

    //      Задача 2 (Середній рівень)
    // Створи масив із N елементів (наприклад, 6 або 8).
    // Переверни масив у зворотному порядку (реверс): перший елемент стає останнім, другий — передостаннім і т.д.
    // Умова: Зроби це без використання додаткового масиву (in-place), змінюючи елементи безпосередньо у початковому.
    int arr2[6] = {10, 20, 30, 40, 50, 60};

    for (int i = 0; i < 6; i++) {
        std::cout << arr2[i] << ' ';
    }
    std::cout << std::endl;
    for (int i = 6; i > 0; i--) {
        std::cout << arr2[i-1] << ' ';
    }
    // -----------------------------------------------------------------------------


    //      Задача 3 (Складна)
    // Напиши програму, яка знаходить другий за величиною елемент у масиві.
    // Наприклад, для масиву {12, 35, 1, 10, 34, 1} відповідь: 34.
    // Умова: Не використовувати сортування (std::sort). Знайди його за один прохід масиву за час O(n).
    int arr3[6] = {12, 35, 1, 100, 88, 1};
    int max1 = 0, max2 = 0;
        for (int i = 0; i < 6; i++) {
            if (arr3[i] > max1) {
                max2 = max1;
                max1 = arr3[i];
            } else if (arr3[i] > max2 && arr3[i] != max1) {
                max2 = arr3[i];
            }
    }
    std::cout << '\n' << "Максимальне число в масиві - " << max1;
    std::cout << '\n' << "Друге максимальне число в масиві - " << max2 << std::endl;
    // ------------------------------------------------------------------------------

    //     Умова Задачі 4:
    // Реалізуй циклічний зсув елементів масиву вправо на k позицій.
    // Наприклад, масив {1, 2, 3, 4, 5} при k=2 має перетворитися на {4, 5, 1, 2, 3}.
    // Умова: Працюємо "на місці" (без створення другого масиву) та за O(n) часу.
    // * Підказка (лайфхак трьох реверсів):
    // Для того, щоб зсунути масив розміром N вправо на k позицій:
    // Переверни весь масив.
    // Переверни перші k елементів.
    // Переверни решту N−k елементів.*
                    // int arr4[5] = {1, 2, 3, 4, 5};
                    // int k = 2;
                    //
                    // // Реверс всього масиву
                    // for (int i = 0; i < (5 / 2); i++) {
                    //     std::swap( arr4[i], arr4[5-i-1] );
                    // }
                    //
                    // // Реверс перших k елементів
                    // for (int i = 0; i < (k - 1); i++) {
                    //     std::swap( arr4[i], arr4[i+1] );
                    // }
                    //
                    // // Вертаємо назад решту N елементів
                    // for (int i = 0; i < (5 / 2); i++) {
                    //     std::swap( arr4[i + k], arr4[5-i-1] );
                    // }
                    //
                    // // Виводимо результат
                    // for (int i = 0; i < 5; i++) {
                    //     std::cout << arr4[i] << ' ';
                    // }
    void reverseArray(int arr[], int start, int end);                       // прототип ф-ці.
    int arr4[5] = {1, 2, 3, 4, 5};
    int n = 5;
    int k = 2;

    // Страховка: якщо k більше за розмір масиву n
    k = k % n;

    // 1. Реверс усього масиву: {1, 2, 3, 4, 5} -> {5, 4, 3, 2, 1}
    reverseArray(arr4, 0, n - 1);

    // 2. Реверс перших k елементів: {5, 4} -> {4, 5}
    reverseArray(arr4, 0, k - 1);

    // 3. Реверс решти (n - k) елементів: {3, 2, 1} -> {1, 2, 3}
    reverseArray(arr4, k, n - 1);

    // Вивід результату: {4, 5, 1, 2, 3}
    for (int i = 0; i < n; i++) {
        std::cout << arr4[i] << ' ';
    }
    // ------------------------------------------------------------------------------

    return 0;
}

// Функція для розвороту частини масиву від індексу start до end
void reverseArray(int arr[], int start, int end) {
    while (start < end) {
        std::swap(arr[start], arr[end]);
        start++;
        end--;
    }
}